---
sudo: required
dist: bionic

notifications:
  slack:
    on_failure: always

matrix:
  fast_finish: true
  include:

    - name: "kubeflow kind w snapped kubectl helm on focal amd64" 
      dist: focal
      arch: amd64
      addons:
        snaps:
          - name: kubectl
            confinement: classic
            channel: latest/stable
          - name: helm
            confinement: classic
            channel: latest/stable
      language: python
      python: 3.7
      before_install:
        - pip3 install virtualenv
        - virtualenv -p $(which python3) ~venvpy3
        - source ~venvpy3/bin/activate
        - pip install -r requirements.txt
      script:
        - sudo make deploy-kind
        - sudo kind create cluster --name kubeflow-testing
        - sudo kubectl config use-context kind-kubeflow-testing
        - sudo make deploy-kubeflow
        - sudo kind delete cluster --name kubeflow-testing
      after_success:
        - deactivate

# /usr/local/bin/kind: 8: /usr/local/bin/kind: Syntax error: ")" unexpected
    # - name: "kubeflow kind w snapped kubectl helm on bionic arm64" 
    #   dist: bionic
    #   arch: arm64
    #   addons:
    #     snaps:
    #       - name: kubectl
    #         confinement: classic
    #         channel: latest/stable
    #       - name: helm
    #         confinement: classic
    #         channel: latest/stable
    #   language: python
    #   python: 3.7
    #   before_install:
    #     - pip3 install virtualenv
    #     - virtualenv -p $(which python3) ~venvpy3
    #     - source ~venvpy3/bin/activate
    #     - pip install -r requirements.txt
    #   script:
    #     - sudo make deploy-kind
    #     - sudo kind create cluster --name kubeflow-testing
    #     - sudo kubectl config use-context kind-kubeflow-testing
    #     - sudo make deploy-kubeflow
    #     - sudo kind delete cluster --name kubeflow-testing
    #   after_success:
    #     - deactivate

    - name: "kubeflow kind w snapped kubectl helm on bionic amd64" 
      dist: bionic
      arch: amd64
      addons:
        snaps:
          - name: kubectl
            confinement: classic
            channel: latest/stable
          - name: helm
            confinement: classic
            channel: latest/stable
      language: python
      python: 3.7
      before_install:
        - pip3 install virtualenv
        - virtualenv -p $(which python3) ~venvpy3
        - source ~venvpy3/bin/activate
        - pip install -r requirements.txt
      script:
        - sudo make deploy-kind
        - sudo kind create cluster --name kubeflow-testing
        - sudo kubectl config use-context kind-kubeflow-testing
        - sudo make deploy-kubeflow
        - sudo kind delete cluster --name kubeflow-testing
      after_success:
        - deactivate
